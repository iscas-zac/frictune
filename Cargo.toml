[package]
name = "frictune"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sqlx = { features = [ "runtime-async-std", "sqlite" ] }
futures = "0.3"
serde = "1.0"
serde_derive = "1.0"
confy = "0.5.1"
clap = { version = "4.2.4", features = ["derive"] }